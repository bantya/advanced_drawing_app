const feature = {
  transform: {
    // Enables the shape rotation handle.
    ROTATE_HANDLE: true,
  },

  wheel_adjustment: {
    // Enables or disables the property value adjustments with the mouse wheel.
    ENABLED: true,

    // Enables the property value change by the `alt key + mousewheel`.
    // The value is incremented by a small amount of ±0.1.
    ENABLED_ALT: true,
  },
}
